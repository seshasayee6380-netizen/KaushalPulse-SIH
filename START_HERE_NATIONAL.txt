KAUSHALPULSE — NATIONAL WORKING BUILD

1. Run run_backend.bat
2. Open OPEN_THIS_FIRST.html
3. Hard refresh once with Ctrl+F5 if an older build was open.

National selector:
State -> District

All known national locations are loaded into the local database. When internet is available the backend refreshes the national reference; the bundled snapshot keeps the selector usable offline.

Prototype market rows are clearly classified as prototype-seeded data. They are there so every district can be clicked and the full workflow can run without a blank page.
